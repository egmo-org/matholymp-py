diff -rupN roundup-2.6.0.orig/roundup/scripts/roundup_server.py roundup-2.6.0/roundup/scripts/roundup_server.py
--- roundup-2.6.0.orig/roundup/scripts/roundup_server.py	2026-06-01 18:52:44.000000000 +0000
+++ roundup-2.6.0/roundup/scripts/roundup_server.py	2026-09-13 17:29:17.385429998 +0000
@@ -887,7 +887,7 @@ class ServerConfig(configuration.Config)
             # This sets the socket to non-blocking. SSL needs a blocking
             # socket, so we do this only for non-SSL connections.
             if hasattr(socket, 'setdefaulttimeout'):
-                socket.setdefaulttimeout(60)
+                socket.setdefaulttimeout(300)
             base_server = http_.server.HTTPServer
 
         # obtain request server class
